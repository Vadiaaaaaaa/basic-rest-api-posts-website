<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scsale=1.0">
    <title>posts</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Quora Posts</h1>

    <% for (post of posts){ %>
        <div class="post">
            <h3>@<%= post.username %></h3>
            <h5><%= post.content %></h5>
            <a href=<%="http://localhost:8080/posts/"+post.id%>>Details</a>
            <a href=<%="http://localhost:8080/posts/"+post.id + "/edit"%>>Edit</a>
            <form method="post" action=<%="http://localhost:8080/posts/"+post.id + "?_method=DELETE"%>>
                <button>Delete</button>
            </form>
            <a href=""></a>
        </div>
        
    <% } %>
    <a href="http://localhost:8080/posts/new">CREATE NEW POST</a>
</body>
</html>